参考UGUI源码

公有成员：
    小写开头
    public RectTransform rectTransform
私有成员：
    m_前缀+大写开头
    private Image m_Image

一、命名规则
1、类的成员变量，私有变量一律一个下划线开头，公共变量不需要下划线
2、类的私有变量在当前类之外不可以直接变量访问，请实现一个Get接口
3、客户端所有命名使用Camel命名规则，变量首字母用小写，类型首字母用大写
4、服务器过来的数据值使用小写，客户端除了local变量，禁止使用全小写(除非只有一个单词)
